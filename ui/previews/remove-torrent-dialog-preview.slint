import { MainWindow } from "../main-window.slint";
export component RemoveTorrentDialogPreview inherits MainWindow { width: 800px; height: 600px; remove-dialog-open: true; remove-dialog-name: "A selected torrent with a long Unicode name пример"; }
